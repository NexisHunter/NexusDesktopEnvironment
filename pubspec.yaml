name: nexus_desktop_environment
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^2.1.5
  logging: <=2.0.0
  provider: ^4.1.5

  nexusos_sdk:
    git:
      url: git@github.com:Nexushunter/nexusos_sdk.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
